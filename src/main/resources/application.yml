spring:
  datasource:
    url: jdbc:mariadb://localhost:3333/test
    driver-class-name: org.mariadb.jdbc.Driver
    username: 'root'
    password: '0000'
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

  batch:
    initialize-schema: always